# Agent scope for this run

Processed unique upstream gaps not already covered by open PRs #66-#70.

## Tools processed

### Windsurf / Devin Desktop
- Added MDM publisher allowlist format, Linux `policy.json` templates, and Devin CLI team settings across Baseline/Moderate/Strict.
- Updated README, rationale, secure checklist, and admin-controls reference.
- Pointed discovery watchers at official Devin enterprise policy and CLI team-settings docs.

### Codex Desktop
- Repaired moved config reference watcher (`docs/config.md`). No duplicate tier edits (see PR #69).

## Deferred / no duplicate edit
- Claude Code agent/env controls → open PRs #66/#68
- OpenAI Platform OpenAPI terms → open PR #69
- Claude Desktop Browser/SSH → open PR #70
- Claude Desktop MCP helper env vars → Claude Code runtime, not Desktop MDM

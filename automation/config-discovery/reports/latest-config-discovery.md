# Config Discovery Report

No discovery run has produced source changes yet.

The scheduled workflow updates this file when a watched upstream source changes. Reviewers should then use `automation/config-discovery/agent-prompt.md` to decide whether any config or documentation files need an update.

# Continue.dev — Secure global configuration
# Deploy to: ~/.continue/config.yaml

schema: v1

# Model configuration — use environment variables for API keys, NEVER inline.
models:
  - name: "default"
    provider: "openai"
    model: "gpt-4o"
    apiKey: "${{ secrets.OPENAI_API_KEY }}"
    requestOptions:
      timeout: 30
      headers:
        X-Request-Source: "continue-dev"

# Tab autocomplete model
tabAutocompleteModel:
  provider: "openai"
  model: "gpt-4o-mini"
  apiKey: "${{ secrets.OPENAI_API_KEY }}"

# Embeddings for @codebase indexing
embeddingsProvider:
  provider: "openai"
  model: "text-embedding-3-small"
  apiKey: "${{ secrets.OPENAI_API_KEY }}"

# Context providers — limit to safe, read-only providers
contextProviders:
  - name: "code"
  - name: "docs"
  - name: "diff"
  - name: "terminal"
  - name: "tree"
  - name: "codebase"

# WHAT: Approved MCP servers that Continue can launch as tools.
# WHY: Empty by default so no external tool server is trusted until IT reviews
#      the package, command, scopes, network access, and logging.
# BREAKS IF REMOVED: Admins may miss MCP governance and developers may add
#      unreviewed local servers with filesystem, shell, or API access.
mcpServers: []

# Disable telemetry
allowAnonymousTelemetry: false

# Indexing enabled for codebase search; set to true for untrusted codebases
disableIndexing: false
